<Window x:Class="AxisControlSample.CMLogYWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d3="http://research.microsoft.com/DynamicDataDisplay/1.0"
        Title="LogYWindow" Height="300" Width="300">
    <Window.Resources>
        <DataTemplate x:Key="followMarkerTemplate">
            <Grid>
                <Rectangle RadiusX="5" RadiusY="5" Fill="LightGray" Stroke="DarkGray">
                    <Rectangle.Effect>
                        <DropShadowEffect Color="DarkGray" Direction="290" ShadowDepth="4"/>
                    </Rectangle.Effect>
                </Rectangle>
                <StackPanel Orientation="Vertical" Margin="3">
                    <TextBlock>Vol: <TextBlock Text="{Binding Data.X}"/></TextBlock>
                    <TextBlock>On: <TextBlock Text="{Binding Data.Y}"/></TextBlock>
                </StackPanel>
            </Grid>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        
    </Grid>
</Window>
